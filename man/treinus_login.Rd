% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{treinus_login}
\alias{treinus_login}
\title{Login to Treinus (step 1 of authentication)}
\usage{
treinus_login(
  email = NULL,
  password = NULL,
  remember_me = TRUE,
  base_url = "https://webapp.treinus.com.br"
)
}
\arguments{
\item{email}{Character string with your Treinus email/username.
Can also be set via \code{TREINUS_EMAIL} environment variable.}

\item{password}{Character string with your Treinus password.
Can also be set via \code{TREINUS_PASSWORD} environment variable.}

\item{remember_me}{Logical. Keep session alive for future requests.
Default is TRUE.}

\item{base_url}{Character string with the base URL for Treinus.
Default is "https://webapp.treinus.com.br".}
}
\value{
Either:
\itemize{
\item A \code{treinus_session} object if no team selection is needed
\item A \code{treinus_pending_login} object with \verb{$teams} tibble if team selection is needed
}
}
\description{
Performs the initial login to Treinus. If the user belongs to multiple teams,
returns a pending login object that can be used with \code{\link[=treinus_select_team]{treinus_select_team()}}.
This allows Shiny apps to present a team selection UI.
}
\examples{
\dontrun{
# For Shiny apps - login first, then check if team selection needed
login_result <- treinus_login(email, password)

if (inherits(login_result, "treinus_pending_login")) {
  # Show team selection UI using login_result$teams
  print(login_result$teams)
  # Then complete with treinus_select_team()
} else {
  # Already authenticated, use login_result as session
}
}

}
\seealso{
\code{\link[=treinus_select_team]{treinus_select_team()}}, \code{\link[=treinus_auth]{treinus_auth()}}
}
