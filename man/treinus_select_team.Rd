% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{treinus_select_team}
\alias{treinus_select_team}
\title{Select a team (step 2 of authentication)}
\usage{
treinus_select_team(pending_login, team = NULL)
}
\arguments{
\item{pending_login}{A \code{treinus_pending_login} object from \code{\link[=treinus_login]{treinus_login()}}}

\item{team}{Team ID (numeric) or team name (character) to select.
If NULL, uses the first (default) team.}
}
\value{
A \code{treinus_session} object that can be used for API calls.
}
\description{
Completes authentication by selecting a team. Use after \code{\link[=treinus_login]{treinus_login()}}
when the user belongs to multiple teams.
}
\examples{
\dontrun{
# Login first
login_result <- treinus_login(email, password)

# Check available teams
if (inherits(login_result, "treinus_pending_login")) {
  print(login_result$teams)
  #   id                    name
  #   600                   Team A
  #   601                   Team B

  # Select by name or ID
  session <- treinus_select_team(login_result, team = "Team A")
  # or
  session <- treinus_select_team(login_result, team = 600)
}
}

}
\seealso{
\code{\link[=treinus_login]{treinus_login()}}, \code{\link[=treinus_auth]{treinus_auth()}}
}
