% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{treinus_auth}
\alias{treinus_auth}
\title{Authenticate with Treinus}
\usage{
treinus_auth(
  email = NULL,
  password = NULL,
  team_id = NULL,
  remember_me = TRUE,
  base_url = "https://webapp.treinus.com.br"
)
}
\arguments{
\item{email}{Character string with your Treinus email/username.
Can also be set via \code{TREINUS_EMAIL} environment variable.}

\item{password}{Character string with your Treinus password.
Can also be set via \code{TREINUS_PASSWORD} environment variable.}

\item{team_id}{Integer team ID to select if the user belongs to multiple teams.
Can also be set via \code{TREINUS_TEAM_ID} environment variable.
If NULL and user has multiple teams, uses the first (default) team.}

\item{remember_me}{Logical. Keep session alive for future requests.
Default is TRUE.}

\item{base_url}{Character string with the base URL for Treinus.
Default is "https://webapp.treinus.com.br".}
}
\value{
A \code{treinus_session} object that can be used for API calls.
The session stores \code{team_id} as an attribute for use in subsequent calls.
}
\description{
Creates an authenticated session with the Treinus webapp. This function
handles the ASP.NET WebForms authentication flow, including extracting
ViewState tokens and managing session cookies.
}
\details{
For Shiny apps or other cases where you need to present team selection UI,
use \code{\link[=treinus_login]{treinus_login()}} and \code{\link[=treinus_select_team]{treinus_select_team()}} instead.
}
\examples{
\dontrun{
# Authenticate using environment variables (recommended)
# Set in .Renviron: TREINUS_EMAIL, TREINUS_PASSWORD, TREINUS_TEAM_ID
session <- treinus_auth()

# Or provide credentials directly
session <- treinus_auth(
  email = "your.email@example.com",
  password = "your-password",
  team_id = 2994
)
}

}
\seealso{
\code{\link[=treinus_login]{treinus_login()}}, \code{\link[=treinus_select_team]{treinus_select_team()}} for Shiny apps
}
